# Chess Rating Predictor

Machine learning project that tries to predict a chess player's
approximate rating (Elo) from several of their games in PGN format.

The core idea: the outcome of a single game is noisy (a bad day, an odd
opponent), but averaging move-quality features over 5-15 games of the
same player gives a much more stable signal to predict their rating from.

## Project structure

```
chess-rating-predictor/
├── data/
│   ├── raw/            # original PGNs (not committed, except the small samples)
│   └── processed/      # CSVs/PGNs generated by the scripts
├── src/
│   ├── parse_pgn.py             # MVP: PGN -> per-game CSV with basic features, no engine
│   ├── extract_sample.py        # cuts the first N games out of a huge Lichess .pgn.zst
│   ├── select_games_by_player.py # scans a full Lichess dump and selects 5-15 games per player
│   ├── analyze_with_stockfish.py # runs Stockfish (in parallel) to get move-quality features
│   ├── aggregate_by_player.py   # averages per-game features into one row per player
│   └── train_baseline.py        # trains a baseline model (Ridge / Random Forest)
├── requirements.txt
└── README.md
```

## Setup

1. Create a virtual environment:
   ```bash
   python -m venv .venv
   source .venv/bin/activate      # on Windows: .venv\Scripts\activate
   ```
2. Install the dependencies:
   ```bash
   pip install -r requirements.txt
   ```
3. Download [Stockfish](https://stockfishchess.org/download/) and place the
   executable somewhere the scripts can find it (see `STOCKFISH_PATH` in
   `analyze_with_stockfish.py` / `benchmark_stockfish.py`).

## Quick check with the sample PGN

The repo includes `data/raw/sample.pgn` (3 sample games) and
`data/raw/lichess_sample.pgn` (20,000 real games) so you can verify the
basic pipeline works without downloading anything:

```bash
python src/parse_pgn.py --input data/raw/sample.pgn --output data/processed/sample_games.csv
```

This should generate `data/processed/sample_games.csv` with one row per
game (White/Black rating, opening, number of plies, result).

## Full pipeline with real Lichess data

1. **Download a Lichess dump.** Go to
   [database.lichess.org](https://database.lichess.org/) and grab one of
   the monthly files (`.pgn.zst`, tens of GB compressed). Every script
   here streams the decompression on the fly -- none of them ever write
   the fully decompressed file to disk, so you only need free space for
   the compressed download itself.

2. **Select a manageable, player-grouped subset:**
   ```bash
   python src/select_games_by_player.py
   ```
   Edit `ZST_PATH`, `MIN_GAMES_PER_PLAYER`, `MAX_GAMES_PER_PLAYER` and
   `TARGET_PLAYERS` at the top of the script to match your setup. This
   does two passes over the dump (counting eligible players, then
   collecting their games) and writes `data/processed/selected_games.pgn`.
   It's meant to be left running unattended: progress is logged to
   `logs/` in UTF-8, and if interrupted it still saves whatever was
   already selected.

3. **Extract move-quality features with Stockfish:**
   ```bash
   python src/analyze_with_stockfish.py \
       --input data/processed/selected_games.pgn \
       --output data/processed/games_engine.csv \
       --max-games 20000
   ```
   This runs several Stockfish processes in parallel (`--workers`,
   defaults to roughly half the machine's logical CPU count, since
   Stockfish gets little benefit from hyperthreading). It's the slowest
   step by far -- use `benchmark_stockfish.py` first to estimate how many
   games fit in the time you have.

4. **Aggregate by player:**
   ```bash
   python src/aggregate_by_player.py \
       --input data/processed/games_engine.csv \
       --output data/processed/players.csv
   ```
   Collapses the per-game CSV into one row per player, averaging their
   features over their games.

5. **Train the baseline model:**
   ```bash
   python src/train_baseline.py
   ```
   Trains a Ridge regression and a Random Forest on `players.csv` and
   reports MAE and R² for both.

## Next steps

- [ ] Compare more models (XGBoost, gradient boosting) and tune
      hyperparameters.
- [ ] Try deeper Stockfish analysis (depth > 10) to see if it improves
      ACPL accuracy enough to be worth the extra time.
- [ ] Package a simple Streamlit demo: upload a few PGNs, get an
      estimated rating back.
